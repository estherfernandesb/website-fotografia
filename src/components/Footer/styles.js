import styled from "styled-components";

export const Rodape = styled.footer`
    h2{
        background-color: var(--primary);
        color: var(--third);
        font-size: 16px;
        text-align: center;
        padding: 2em;
        font-weight: 200;
        margin: 0;
        line-height: -0.5;
        }
`